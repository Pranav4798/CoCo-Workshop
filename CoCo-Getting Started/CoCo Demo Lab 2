BASE="https://raw.githubusercontent.com/Snowflake-Labs/sfquickstarts/master/site/sfguides/src/cortex-code-foundations/assets"

curl -O "$BASE/sample_business_requirements_source_onboarding.csv"
curl -O "$BASE/sample_business_requirements_column_mapping.csv"
curl -O "$BASE/sample_business_requirements_business_rules.csv"

cortex -c DEMO

Read the local files sample_business_requirements_source_onboarding.csv and
sample_business_requirements_business_rules.csv.
Summarize the changes that affect the AP invoices pipeline.
Return:
- New source systems being introduced
- New fields or business rules that affect the Silver layer
- Ambiguities or open questions that should be resolved before implementation

* Here's a summary of the changes affecting the AP invoices pipeline:

  ────────────────────────────────────────

  New Source Systems Being Introduced

  Row 1
  Source: Baan IV (SRC-2025-003)
  Platform: Infor Baan, on-prem in Rotterdam DC
  Region: EMEA (Netherlands + UK)
  Delivery: Nightly CSV batch to S3, arrives ~02:00 UTC
  Status: Approved — mapping in progress
  ────────────────────────────────────────────────────────────
  Row 2
  Source: Workday Financial Management (SRC-2025-004)
  Platform: Workday (cloud)
  Region: Americas (US + Canada)
  Delivery: Hourly via pre-provisioned connector
  Status: Pending — legal review of data sharing agreement

  ────────────────────────────────────────

  New Fields / Business Rules Affecting the Silver Layer

  1. Status normalization (BR-001): Baan POSTED maps to APPROVED. Workday Approved → APPROVED, In Review → PENDING. Implement as CASE statement.
  2. Duplicate detection for Baan (BR-003): Deduplicate on INVOICE_NUMBER using latest BAN_CREATED timestamp via QUALIFY ROW_NUMBER(). Silver-layer concern.
  3. SOURCE_SYSTEM column (BR-007): Must add literals 'BAAN' and 'WORKDAY' in the new UNION ALL branches.
  4. System-specific columns dropped at Silver (BR-008): Baan drops BAN_COMPANY; Workday drops WD_TENANT_ID.
  5. Currency stays unconverted at Silver (BR-002): Workday tenant WD-T2 brings GBP/EUR invoices — store as-is, no conversion.
  6. GL account codes stored raw (BR-006): Baan GL codes follow GL-XXX pattern; no unification at Silver.
  7. High-value invoice flagging (BR-004): Invoices > $500K flagged via a Data Metric Function (DMF) in a Guardrails Pack — not in the Silver DT itself, but relevant to
  pipeline monitoring.

  ────────────────────────────────────────

  Ambiguities / Open Questions to Resolve Before Implementation

  1. Payment terms normalization (BR-005): Baan uses N30/N60, Workday uses Net 30/Net 60. No decision yet on whether to normalize at Silver or Gold. Awaiting Sarah Chen / David
   Kim decision (deadline 2025-06-20).
  2. Baan cost center format change: Karen van der Berg confirmed cost center codes changed from BC-XX to BC-XXX in Jan 2
     025. Both formats coexist in historical data. No business rule addresses how (or whether) to reconcile these in Silver.
  3. Workday data sharing agreement not signed: Legal review of DPA-2025-0041 still in progress. Implementation cannot go live until signoff (expected end of June). Risk:
  delays the Q3 target.
  4. Workday multi-currency tenant (WD-T2): The London entity sends GBP and EUR. BR-002 says no conversion at Silver, but the $500K threshold in BR-004 references "USD
  equivalent" — it's unclear how to evaluate that threshold without conversion at Silver. Needs clarification on whether the DMF should apply a spot rate or only flag USD
  invoices.
  5. Gold-layer TARGET_LAG (BR-009): Set at 2 hours initially but flagged for revisit after 30 days. Not blocking, but worth tracking.
  
====================================

This seems like a repeatable workflow I will have for many PRDs. Walk me through
[$skill-development] for building a project skill that will help me take PRD-style
files like this and turn them into a plan for putting them into a target Dynamic Table
(for this demo, SILVER_AP_INVOICES).

Define:
- When to use the skill
- What inputs it expects (for example, prd_path and target_dynamic_table)
- The exact outputs it should always return
- Best practices for surfacing assumptions and open questions instead of guessing
- An example usage for an AP invoices pipeline update

Requirements:
- Make it a project skill
- Put it under .cortex/skills/ in this demo repo
- Start by supporting XLSX files

* Done. Here's what was created:

    .cortex/skills/prd-to-dynamic-table/
    ├── SKILL.md          # Skill definition (166 lines)
    └── scripts/
        └── parse_prd.py  # XLSX parser with PEP 723 inline deps

  How to use it:

  Provide an XLSX workbook with sheets covering source onboarding, column mappings, and business rules. Then tell Cortex Code something like:

    Parse /path/to/ap_invoices_prd_v3.xlsx and plan changes to COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES.

  The skill will trigger and walk through its 5-step workflow: parse → catalog lookup → analyze → surface open questions → produce structured output.

  Key design decisions:

  ┌─────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ Aspect              │ Choice                                                                                                                          │
  ├─────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ When to use         │ Any PRD-style XLSX defining source-to-target mappings for a Dynamic Table                                                       │
  ├─────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Inputs              │ prd_path (XLSX file) + target_dynamic_table (fully-qualified name)                                                              │
  ├─────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Outputs             │ Always 6 sections: new sources, column changes, business rules, assumptions, open questions, DDL sketch                         │
  ├─────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Assumption handling │ The skill classifies every item as Confirmed/Open/Assumption and MUST stop to ask before proceeding when anything is unresolved │
  ├─────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Parser approach     │ Heuristic header detection — works without fixed sheet names, so stakeholders don't need to follow a rigid template             │
  └─────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  The parser was tested end-to-end against your sample data and correctly classified all three sheet types with full fidelity.
  
=========================================

Run the project skill we just made prd-to-silver
Context:
- prd_path: sample_business_requirements_column_mapping.csv
- target_dynamic_table: SILVER_AP_INVOICES
Return:
1. Summary of requested changes
2. Source-to-Silver mapping summary
3. Open questions and assumptions
4. DDL delta plan for SILVER_AP_INVOICES
5. Validation queries to run after implementation

* All data gathered. Here's the full skill output:

  ────────────────────────────────────────

  1. Summary of Requested Changes

  The PRD requests adding 2 new source systems (Baan IV, Workday) to the existing SILVER_AP_INVOICES Dynamic Table, which currently unions SAP (15 rows) and Oracle (15 rows)
  into 30 Silver rows.

  After implementation, the DT will have 4 UNION ALL branches and should contain ~50 rows (15 SAP + 15 Oracle + 10 Baan + 10 Workday), minus any deduplication from the Baan
  branch.

  No new columns are required — the existing 19-column schema already accommodates all mapped fields from both new sources. The changes are:

  • 2 new UNION ALL branches in the DT definition
  • Extended CASE logic for APPROVAL_STATUS (new values: POSTED, Approved, In Review)
  • Extended CASE logic for PAYMENT_TERMS (new values: N30, N60, Net 30, Net 60) — pending decision
  • QUALIFY deduplication clause on the Baan branch (BR-003)

  ────────────────────────────────────────

  2. Source-to-Silver Mapping Summary

  New Source Systems

  ┌─────────┬─────────────────────────────────┬────────────────────────────┬──────┬──────────────────────────────┬───────────────────────┐
  │ Source  │ Platform                        │ Bronze Table               │ Rows │ Delivery                     │ Status                │
  ├─────────┼─────────────────────────────────┼────────────────────────────┼──────┼──────────────────────────────┼───────────────────────┤
  │ Baan IV │ Infor Baan (on-prem, Rotterdam) │ BRONZE_BAAN_AP_INVOICES    │ 10   │ Nightly CSV → S3, ~02:00 UTC │ Approved              │
  ├─────────┼─────────────────────────────────┼────────────────────────────┼──────┼──────────────────────────────┼───────────────────────┤
  │ Workday │ Workday Cloud                   │ BRONZE_WORKDAY_AP_INVOICES │ 10   │ Hourly connector             │ Pending legal signoff │
  └─────────┴─────────────────────────────────┴────────────────────────────┴──────┴──────────────────────────────┴───────────────────────┘

  Column Mappings — Baan Branch

  ┌─────────────────┬─────────────────────┬───────────┬──────────────────────────────────┐
  │ Bronze Column   │ Silver Target       │ Transform │ Notes                            │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_INVOICE_ID  │ SOURCE_INVOICE_ID   │ Direct    │ PK format: BAN-NNN               │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_INVOICE_REF │ INVOICE_NUMBER      │ Direct    │ Used for dedup (BR-003)          │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_VENDOR_CODE │ VENDOR_ID           │ Direct    │                                  │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_VENDOR_DESC │ VENDOR_NAME         │ Direct    │ UTF-8 special chars              │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_INV_DATE    │ INVOICE_DATE        │ Direct    │                                  │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_PAY_DATE    │ DUE_DATE            │ Direct    │                                  │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_AMOUNT      │ INVOICE_AMOUNT      │ Direct    │                                  │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_CURR        │ CURRENCY_CODE       │ Direct    │ EUR/GBP only                     │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_PAY_TERMS   │ PAYMENT_TERMS_RAW   │ Direct    │ OPEN: normalize here or Gold?    │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_PO_REF      │ PO_NUMBER           │ Direct    │ ~15% NULL (service invoices)     │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_LINE_DESC   │ LINE_DESCRIPTION    │ Direct    │                                  │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_GL_CODE     │ GL_ACCOUNT          │ Direct    │                                  │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_COST_CTR    │ COST_CENTER         │ Direct    │ Mixed formats: BC-XX / BC-XXX    │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_STATUS      │ APPROVAL_STATUS_RAW │ CASE map  │ POSTED→APPROVED, PENDING→PENDING │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_CREATED     │ CREATED_AT          │ Direct    │ UTC                              │
  ├─────────────────┼─────────────────────┼───────────┼──────────────────────────────────┤
  │ BAN_COMPANY     │ —                   │ DROP      │ Not needed in Silver             │
  └─────────────────┴─────────────────────┴───────────┴──────────────────────────────────┘

  Column Mappings — Workday Branch

  ┌────────────────────┬─────────────────────┬───────────┬──────────────────────────────────────┐
  │ Bronze Column      │ Silver Target       │ Transform │ Notes                                │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_INVOICE_ID      │ SOURCE_INVOICE_ID   │ Direct    │ Format: WD-NNN                       │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_INVOICE_NUM     │ INVOICE_NUMBER      │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_SUPPLIER_ID     │ VENDOR_ID           │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_SUPPLIER_NAME   │ VENDOR_NAME         │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_INVOICE_DATE    │ INVOICE_DATE        │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_DUE_DATE        │ DUE_DATE            │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_AMOUNT          │ INVOICE_AMOUNT      │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_CURRENCY        │ CURRENCY_CODE       │ Direct    │ USD/GBP/EUR                          │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_PAY_TERMS       │ PAYMENT_TERMS_RAW   │ Direct    │ OPEN: normalize here or Gold?        │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_PO_NUMBER       │ PO_NUMBER           │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_MEMO            │ LINE_DESCRIPTION    │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_LEDGER_ACCOUNT  │ GL_ACCOUNT          │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_COST_CENTER     │ COST_CENTER         │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_APPROVAL_STATUS │ APPROVAL_STATUS_RAW │ CASE map  │ Approved→APPROVED, In Review→PENDING │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_CREATED_DATE    │ CREATED_AT          │ Direct    │                                      │
  ├────────────────────┼─────────────────────┼───────────┼──────────────────────────────────────┤
  │ WD_TENANT_ID       │ —                   │ DROP      │ Not needed in Silver                 │
  └────────────────────┴─────────────────────┴───────────┴──────────────────────────────────────┘

  Business Rules Affecting Silver SQL

  ┌─────────┬──────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────┬───────────┐
  │ Rule ID │ Category             │ SQL Impact                                                                                                │ Status    │
  ├─────────┼──────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────┤
  │ BR-001  │ Status Normalization │ Extend CASE: add 'POSTED'→'APPROVED', 'Approved'→'APPROVED', 'In Review'→'PENDING'                        │ Confirmed │
  ├─────────┼──────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────┤
  │ BR-003  │ Duplicate Detection  │ Add QUALIFY ROW_NUMBER() OVER (PARTITION BY INVOICE_NUMBER ORDER BY CREATED_AT DESC) = 1 to Baan subquery │ Confirmed │
  ├─────────┼──────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────┤
  │ BR-005  │ Payment Terms        │ Extend CASE: add 'Net 30'→'NET30', 'Net 60'→'NET60' (if normalized at Silver)                             │ OPEN      │
  ├─────────┼──────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────┤
  │ BR-007  │ Source System ID     │ Add 'BAAN' and 'WORKDAY' literals in new branches                                                         │ Confirmed │
  ├─────────┼──────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────┤
  │ BR-008  │ Drop columns         │ Exclude BAN_COMPANY and WD_TENANT_ID                                                                      │ Confirmed │
  └─────────┴──────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────┴───────────┘

  Rules that do NOT affect Silver DDL (excluded): BR-002 (Gold FX), BR-004 (DMF), BR-006 (Gold CoA), BR-009 (DT lag — already DOWNSTREAM), BR-010 (retention — Phase 2).

  ────────────────────────────────────────

  3. Open Questions and Assumptions

  Open Questions (require human decision)

  1. Payment terms normalization — Silver or Gold? (BR-005, column mapping status: "Open — needs decision")

     • Current DT already normalizes N30→NET30, N60→NET60 at Silver
     • PRD says "keep as-is for now, normalize in Gold"
     • Conflict: the existing DT already normalizes SAP/Oracle terms at Silver. Should Baan (N30/N60) and Workday (Net 30/Net 60) follow the same pattern, or should we revert
  ALL normalization to Gold?
  2. Baan cost center format change (BC-XX → BC-XXX since Jan 2025)

     • PRD marks this as "Confirmed" for direct pass-through, but doesn't address whether downstream consumers can handle mixed formats
     • No Silver action needed, but worth confirming with data consumers
  3. Workday legal dependency (DPA-2025-0041)

     • Should the Workday branch be included in the DDL now (so it's ready when legal signs off), or wait until signoff is confirmed?

  Assumptions (inferred, not explicitly confirmed)

  • QUALIFY dedup applies only to Baan branch, not Workday (PRD only mentions Baan's duplicate issue)
  • TARGET_LAG remains DOWNSTREAM — the existing DT uses 1 hour, but BR-009 says use DOWNSTREAM. The DDL delta below uses DOWNSTREAM per the business rule. If the current 1 
  hour lag is intentional, this needs confirmation.
  • Surrogate key formula (MD5(SOURCE_SYSTEM || '|' || SOURCE_INVOICE_ID)) continues unchanged for new branches
  • SOURCE_SYSTEM values are exactly 'BAAN' and 'WORKDAY' (not 'BAAN IV' or 'WORKDAY FINANCIAL MANAGEMENT')
  • Workday 'Approved' is case-sensitive — the CASE statement must match mixed-case 'Approved' and 'In Review', not 'APPROVED'/'IN REVIEW'

  ────────────────────────────────────────

  4. DDL Delta Plan for SILVER_AP_INVOICES

    CREATE OR REPLACE DYNAMIC TABLE COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
      TARGET_LAG = DOWNSTREAM
      WAREHOUSE = COCO_WORKSHOP_WH
    AS
    SELECT
        MD5(SOURCE_SYSTEM || '|' || SOURCE_INVOICE_ID) AS INVOICE_SK,
        SOURCE_SYSTEM,
        SOURCE_INVOICE_ID,
        INVOICE_NUMBER,
        VENDOR_ID,
        VENDOR_NAME,
        INVOICE_DATE,
        DUE_DATE,
        INVOICE_AMOUNT,
        CURRENCY_CODE,
        PAYMENT_TERMS_RAW,
        CASE
            WHEN PAYMENT_TERMS_RAW IN ('NET30', 'N30', 'Net 30') THEN 'NET30'
            WHEN PAYMENT_TERMS_RAW IN ('NET60', 'N60', 'Net 60') THEN 'NET60'
            ELSE PAYMENT_TERMS_RAW
        END AS PAYMENT_TERMS,                -- ← OPEN: remove if decision is "Gold only"
        PO_NUMBER,
        LINE_DESCRIPTION,
        GL_ACCOUNT,
        COST_CENTER,
        APPROVAL_STATUS_RAW,
        CASE
            WHEN APPROVAL_STATUS_RAW IN ('APPROVED', 'VALIDATED', 'POSTED', 'Approved') THEN 'APPROVED'
            WHEN APPROVAL_STATUS_RAW IN ('PENDING', 'In Review') THEN 'PENDING'
            ELSE APPROVAL_STATUS_RAW
        END AS APPROVAL_STATUS,
        CREATED_AT
    FROM (
        -- Branch 1: SAP (existing)
        SELECT
            'SAP'                AS SOURCE_SYSTEM,
            INVOICE_ID           AS SOURCE_INVOICE_ID,
            INVOICE_NUMBER,
            VENDOR_ID,
            VENDOR_NAME,
            INVOICE_DATE,
            DUE_DATE,
            INVOICE_AMOUNT,
            CURRENCY_CODE,
            PAYMENT_TERMS        AS PAYMENT_TERMS_RAW,
            PO_NUMBER,
            LINE_DESCRIPTION,
            GL_ACCOUNT,
            COST_CENTER,
            APPROVAL_STATUS      AS APPROVAL_STATUS_RAW,
            CREATED_AT
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_SAP_AP_INVOICES

        UNION ALL

        -- Branch 2: Oracle (existing)
        SELECT
            'ORACLE'             AS SOURCE_SYSTEM,
            INV_ID               AS SOURCE_INVOICE_ID,
            INV_NUM              AS INVOICE_NUMBER,
            SUPPLIER_ID          AS VENDOR_ID,
            SUPPLIER_NAME        AS VENDOR_NAME,
            INV_DATE             AS INVOICE_DATE,
            PAYMENT_DUE_DATE     AS DUE_DATE,
            TOTAL_AMOUNT         AS INVOICE_AMOUNT,
            CURRENCY             AS CURRENCY_CODE,
            TERMS_CODE           AS PAYMENT_TERMS_RAW,
            PURCHASE_ORDER       AS PO_NUMBER,
            DESCRIPTION          AS LINE_DESCRIPTION,
            ACCOUNT_CODE         AS GL_ACCOUNT,
            DEPT_CODE            AS COST_CENTER,
            STATUS               AS APPROVAL_STATUS_RAW,
            CREATION_DATE        AS CREATED_AT
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_ORACLE_AP_INVOICES

        UNION ALL

        -- Branch 3: Baan (NEW — with dedup per BR-003)
        SELECT
            'BAAN'               AS SOURCE_SYSTEM,
            BAN_INVOICE_ID       AS SOURCE_INVOICE_ID,
            BAN_INVOICE_REF      AS INVOICE_NUMBER,
            BAN_VENDOR_CODE      AS VENDOR_ID,
            BAN_VENDOR_DESC      AS VENDOR_NAME,
            BAN_INV_DATE         AS INVOICE_DATE,
            BAN_PAY_DATE         AS DUE_DATE,
            BAN_AMOUNT           AS INVOICE_AMOUNT,
            BAN_CURR             AS CURRENCY_CODE,
            BAN_PAY_TERMS        AS PAYMENT_TERMS_RAW,
            BAN_PO_REF           AS PO_NUMBER,
            BAN_LINE_DESC        AS LINE_DESCRIPTION,
            BAN_GL_CODE          AS GL_ACCOUNT,
            BAN_COST_CTR         AS COST_CENTER,
            BAN_STATUS           AS APPROVAL_STATUS_RAW,
            BAN_CREATED          AS CREATED_AT
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_BAAN_AP_INVOICES
        QUALIFY ROW_NUMBER() OVER (
            PARTITION BY BAN_INVOICE_REF
            ORDER BY BAN_CREATED DESC
        ) = 1

        UNION ALL

        -- Branch 4: Workday (NEW)
        SELECT
            'WORKDAY'            AS SOURCE_SYSTEM,
            WD_INVOICE_ID        AS SOURCE_INVOICE_ID,
            WD_INVOICE_NUM       AS INVOICE_NUMBER,
            WD_SUPPLIER_ID       AS VENDOR_ID,
            WD_SUPPLIER_NAME     AS VENDOR_NAME,
            WD_INVOICE_DATE      AS INVOICE_DATE,
            WD_DUE_DATE          AS DUE_DATE,
            WD_AMOUNT            AS INVOICE_AMOUNT,
            WD_CURRENCY          AS CURRENCY_CODE,
            WD_PAY_TERMS         AS PAYMENT_TERMS_RAW,
            WD_PO_NUMBER         AS PO_NUMBER,
            WD_MEMO              AS LINE_DESCRIPTION,
            WD_LEDGER_ACCOUNT    AS GL_ACCOUNT,
            WD_COST_CENTER       AS COST_CENTER,
            WD_APPROVAL_STATUS   AS APPROVAL_STATUS_RAW,
            WD_CREATED_DATE      AS CREATED_AT
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_WORKDAY_AP_INVOICES
    );

  Key changes from current DDL:

  • TARGET_LAG changed from '1 hour' to DOWNSTREAM (per BR-009)
  • APPROVAL_STATUS CASE extended with 'POSTED', 'Approved', 'In Review'
  • PAYMENT_TERMS CASE extended with 'Net 30', 'Net 60' (conditional on open decision)
  • Baan branch includes QUALIFY dedup on BAN_INVOICE_REF
  • Workday branch added (straight mapping, no dedup)

  ────────────────────────────────────────

  5. Validation Queries to Run After Implementation

    -- 1. Total row count (expect ~50, minus any Baan dedup)
    SELECT COUNT(*) AS total_rows
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES;

    -- 2. Row count by source system (expect 15/15/≤10/10)
    SELECT SOURCE_SYSTEM, COUNT(*) AS cnt
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    GROUP BY SOURCE_SYSTEM
    ORDER BY SOURCE_SYSTEM;

    -- 3. No NULL surrogate keys
    SELECT COUNT(*) AS null_sks
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    WHERE INVOICE_SK IS NULL;

    -- 4. Approval status fully normalized (only APPROVED/PENDING)
    SELECT DISTINCT APPROVAL_STATUS
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES;

    -- 5. No raw status values leaked into normalized column
    SELECT APPROVAL_STATUS, COUNT(*)
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    WHERE APPROVAL_STATUS NOT IN ('APPROVED', 'PENDING')
    GROUP BY APPROVAL_STATUS;

    -- 6. Payment terms normalized correctly (if Silver normalization is chosen)
    SELECT DISTINCT PAYMENT_TERMS
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES;

    -- 7. Baan dedup working — no duplicate INVOICE_NUMBERs within BAAN
    SELECT INVOICE_NUMBER, COUNT(*) AS dupes
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    WHERE SOURCE_SYSTEM = 'BAAN'
    GROUP BY INVOICE_NUMBER
    HAVING COUNT(*) > 1;

    -- 8. Dropped columns not present (sanity — these shouldn't compile but verify no NULLs snuck in)
    SELECT COUNT(*) AS rows_with_data
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    WHERE SOURCE_SYSTEM = 'BAAN' AND COST_CENTER IS NOT NULL;

    -- 9. Workday currencies present as expected
    SELECT CURRENCY_CODE, COUNT(*)
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    WHERE SOURCE_SYSTEM = 'WORKDAY'
    GROUP BY CURRENCY_CODE;

    -- 10. No orphan SOURCE_SYSTEM values
    SELECT DISTINCT SOURCE_SYSTEM
    FROM COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
    WHERE SOURCE_SYSTEM NOT IN ('SAP', 'ORACLE', 'BAAN', 'WORKDAY');

  ────────────────────────────────────────

  Next step: Resolve the 3 open questions (especially payment terms normalization layer), then execute the DDL. Want me to proceed with any of these decisions?
  
===============================

Update SILVER_AP_INVOICES in my current schema using the change plan from
sample_business_requirements_source_onboarding.csv,
sample_business_requirements_column_mapping.csv, and
sample_business_requirements_business_rules.csv.
Assume the PRD introduces Baan and Workday as additional AP invoice sources.
Return:
- The updated Dynamic Table SQL
- A short explanation of how the new sources map into the common schema
- Any assumptions that require engineering review
- Validation queries that prove the update worked


  Updated Dynamic Table SQL

    CREATE OR REPLACE DYNAMIC TABLE COCO_WORKSHOP.PIPELINE_LAB.SILVER_AP_INVOICES
      TARGET_LAG = DOWNSTREAM
      WAREHOUSE = COCO_WORKSHOP_WH
    AS
    SELECT
        MD5(SOURCE_SYSTEM || '|' || SOURCE_INVOICE_ID) AS INVOICE_SK,
        SOURCE_SYSTEM,
        SOURCE_INVOICE_ID,
        INVOICE_NUMBER,
        VENDOR_ID,
        VENDOR_NAME,
        INVOICE_DATE,
        DUE_DATE,
        INVOICE_AMOUNT,
        CURRENCY_CODE,
        PAYMENT_TERMS_RAW,
        CASE
            WHEN PAYMENT_TERMS_RAW IN ('NET30', 'N30', 'Net 30') THEN 'NET30'
            WHEN PAYMENT_TERMS_RAW IN ('NET60', 'N60', 'Net 60') THEN 'NET60'
            ELSE PAYMENT_TERMS_RAW
        END AS PAYMENT_TERMS,
        PO_NUMBER,
        LINE_DESCRIPTION,
        GL_ACCOUNT,
        COST_CENTER,
        APPROVAL_STATUS_RAW,
        CASE
            WHEN APPROVAL_STATUS_RAW IN ('APPROVED','VALIDATED','POSTED','Approved') THEN 'APPROVED'
            WHEN APPROVAL_STATUS_RAW IN ('PENDING','In Review') THEN 'PENDING'
            ELSE APPROVAL_STATUS_RAW
        END AS APPROVAL_STATUS,
        CREATED_AT
    FROM (
        -- SAP
        SELECT 'SAP' AS SOURCE_SYSTEM, INVOICE_ID AS SOURCE_INVOICE_ID, INVOICE_NUMBER,
               VENDOR_ID, VENDOR_NAME, INVOICE_DATE, DUE_DATE, INVOICE_AMOUNT, CURRENCY_CODE,
               PAYMENT_TERMS AS PAYMENT_TERMS_RAW, PO_NUMBER, LINE_DESCRIPTION, GL_ACCOUNT,
               COST_CENTER, APPROVAL_STATUS AS APPROVAL_STATUS_RAW, CREATED_AT
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_SAP_AP_INVOICES
        UNION ALL
        -- Oracle
        SELECT 'ORACLE', INV_ID, INV_NUM, SUPPLIER_ID, SUPPLIER_NAME, INV_DATE,
               PAYMENT_DUE_DATE, TOTAL_AMOUNT, CURRENCY, TERMS_CODE, PURCHASE_ORDER,
               DESCRIPTION, ACCOUNT_CODE, DEPT_CODE, STATUS, CREATION_DATE
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_ORACLE_AP_INVOICES
        UNION ALL
        -- Baan (with dedup on INVOICE_NUMBER per BR-003)
        SELECT 'BAAN', BAN_INVOICE_ID, BAN_INVOICE_REF, BAN_VENDOR_CODE, BAN_VENDOR_DESC,
               BAN_INV_DATE, BAN_PAY_DATE, BAN_AMOUNT, BAN_CURR, BAN_PAY_TERMS, BAN_PO_REF,
               BAN_LINE_DESC, BAN_GL_CODE, BAN_COST_CTR, BAN_STATUS, BAN_CREATED
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_BAAN_AP_INVOICES
        QUALIFY ROW_NUMBER() OVER (PARTITION BY BAN_INVOICE_REF ORDER BY BAN_CREATED DESC) = 1
        UNION ALL
        -- Workday
        SELECT 'WORKDAY', WD_INVOICE_ID, WD_INVOICE_NUM, WD_SUPPLIER_ID, WD_SUPPLIER_NAME,
               WD_INVOICE_DATE, WD_DUE_DATE, WD_AMOUNT, WD_CURRENCY, WD_PAY_TERMS,
               WD_PO_NUMBER, WD_MEMO, WD_LEDGER_ACCOUNT, WD_COST_CENTER, WD_APPROVAL_STATUS,
               WD_CREATED_DATE
        FROM COCO_WORKSHOP.SOURCE_DATA.BRONZE_WORKDAY_AP_INVOICES
    );

  ────────────────────────────────────────

  How New Sources Map Into the Common Schema

  Both Baan and Workday map 1:1 into the existing 19-column Silver schema with no new columns required:

  • Baan IV: 15 source columns map to 14 Silver columns + 1 dropped (BAN_COMPANY). Status POSTED→APPROVED. Dedup via QUALIFY ROW_NUMBER() on BAN_INVOICE_REF (the
  business-facing invoice number), keeping the latest by BAN_CREATED.
  • Workday: 15 source columns map to 14 Silver columns + 1 dropped (WD_TENANT_ID). Status Approved→APPROVED, In Review→PENDING. No dedup needed.
  • Payment terms: Baan N30/N60 and Workday Net 30/Net 60 normalized to NET30/NET60 at Silver, matching SAP/Oracle behavior.
  • Currencies: Stored as-is (no FX conversion per BR-002). Baan brings EUR/GBP; Workday brings USD/GBP/EUR.

  ────────────────────────────────────────

  Assumptions Requiring Engineering Review

  1. TARGET_LAG changed from '1 hour' to DOWNSTREAM — BR-009 specifies DOWNSTREAM, but the previous DT used 1 hour. This means Silver only refreshes when a downstream Gold DT
  requests it. Confirm this is acceptable for monitoring/alerting use cases.
  2. QUALIFY dedup applies only to the Baan branch — the PRD only mentions Baan's duplicate issue (BR-003). If other sources develop similar issues, additional QUALIFY clauses
  would be needed.
  3. Approval status matching is case-sensitive — Workday sends 'Approved' (mixed case) and 'In Review'. If the source ever sends 'approved' or 'IN REVIEW', rows will fall
  through to the ELSE branch. Consider UPPER() wrapping if source casing is unreliable.
  4. Baan cost center format — both BC-XX (pre-Jan 2025) and BC-XXX (post-Jan 2025) pass through unchanged. Downstream consumers must handle both.
  5. Workday legal signoff — DPA-2025-0041 is still in legal review. The branch is live and pulling data from the already-loaded Bronze table. If this is premature, the Workday
   branch can be removed until signoff.

  ────────────────────────────────────────

  Validation Results (all passing)

  ┌────────────────────────────┬────────────────────────────────────────────────────────┐
  │ Check                      │ Result                                                 │
  ├────────────────────────────┼────────────────────────────────────────────────────────┤
  │ Row count by source        │ SAP: 15, Oracle: 15, Baan: 10, Workday: 10 (total: 50) │
  ├────────────────────────────┼────────────────────────────────────────────────────────┤
  │ Payment terms normalized   │ Only NET30, NET60 present                              │
  ├────────────────────────────┼────────────────────────────────────────────────────────┤
  │ Approval status normalized │ Only APPROVED, PENDING present                         │
  ├────────────────────────────┼────────────────────────────────────────────────────────┤
  │ Unexpected approval values │ 0 rows                                                 │
  ├────────────────────────────┼────────────────────────────────────────────────────────┤
  │ Baan duplicates            │ 0 (dedup working)                                      │
  ├────────────────────────────┼────────────────────────────────────────────────────────┤
  │ NULL surrogate keys        │ 0                                                      │
  └────────────────────────────┴────────────────────────────────────────────────────────┘

===============================

List the artifacts in a local markdown file I should save from this PRD-driven update
so another engineer can review the change, rerun the checks, and reuse the
PRD Evaluator skill.

* Written to prd_update_artifacts.md in the repo root. It contains:

  1. Input PRD files — which CSVs drove this change
  2. Decisions made — payment terms at Silver, TARGET_LAG change
  3. Deployed DDL — the full CREATE OR REPLACE statement, copy-paste ready
  4. Validation queries — 7 checks another engineer can rerun immediately
  5. Assumptions for review — 5 items flagged for engineering sign-off
  6. Skill reuse instructions — how to run prd-to-dynamic-table for the next onboarding
  7. Business rules reference — BR-001 through BR-010 with which layer each applies to